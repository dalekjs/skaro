module.exports = function(/*config*/) {
  'use strict';

  // TODO: implement test verification
  // facility that loads all suites, units, tasks and verifies
  // that all interfaces are available. This is supposed to give quick feedback
  // about a missing / malformed plugin, that would otherwise only be thrown
  // at execution time
  console.error('dalek verify not implement yet');
  process.exit(1);
};